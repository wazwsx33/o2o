<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.watermelon.o2o.dao.ProductCategoryDao">
    <select id="queryProdructCategoryList" parameterType="Long" resultType="com.watermelon.o2o.entity.ProductCategory">
        SELECT product_category_id, product_category_name, priority, create_time, shop_id
        FROM tb_product_category
        where shop_id = #{shopId}
        ORDER BY priority DESC
    </select>

    <insert id="batchInsertProductCategory" parameterType="java.util.List">
        INSERT INTO tb_product_category(product_category_name, priority, create_time, shop_id)
        VALUES
        /*item为foreach中每次循环用到的对象，可随意命名*/
        /*separator为分隔符，最终格式为(xxx),(xxx),(xxx)*/
        <foreach collection="list" item="productCategory" index="index" separator=",">
            (
            #{productCategory.productCategoryName},
            #{productCategory.priority},
            #{productCategory.createTime},
            ${productCategory.shopId}
            )
        </foreach>
    </insert>
</mapper>